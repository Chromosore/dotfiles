complete -c del -s i -l interactive -d 'Prompt before deleting every file and directory'
complete -c del -s v -l verbose     -d 'List deleted files'
complete -c del -s f -l force       -d 'Never prompt and ignore unexisting files'
